<h1>Edit Comment</h1>
<%= form_for([@work_out,@new_comment], url: work_out_comments_path(@work_out,@new_comment), method: :post) do |f| %>

	<div class="field">
		<%= f.text_field :body %>
	</div>

	<div class="actions">
		<%= f.submit "登録" %>
	</div>